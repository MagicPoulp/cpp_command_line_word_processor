#include <vector>
#include <string>
#include "Processor.hpp"
#include "Enums.hpp"

using namespace std;

Processor::Processor(vector<processor_step> steps) {
}

void Processor::process(const string& input, const string& output) {
}
